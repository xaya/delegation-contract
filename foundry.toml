[profile.default]
src = "src"
out = "out"
libs = ["lib"]
optimizer = true
optimizer_runs = 1_000_000

[lint]
exclude_lints = [
  "mixed-case-function",
  "screaming-snake-case-immutable",
  "unaliased-plain-import",
]

[etherscan]
polygon = { key = "${APIKEY}" }
